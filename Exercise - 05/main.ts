function quotation(famous_person: string, message: string) {
    console.log(famous_person + " once said: " + "\"" + message + "\"");
}

const famous_person : string = "Sam Altman";
const message : string = "Don\'t let yourself make excuses for not doing the things you want to do.";

quotation(famous_person, message);