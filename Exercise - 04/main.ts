function quotation(name: string, quote: string) {
    console.log(name + "once said: " + "\"" + quote + "\"");
}

const person : string = "Sam Altman";
const quote : string = "Don\'t let yourself make excuses for not doing the things you want to do.";

quotation(person, quote);