function quotation(name, quote) {
    console.log(name + "once said: " + "\"" + quote + "\"");
}
var person = "Sam Altman";
var quote = "Don\'t let yourself make excuses for not doing the things you want to do.";
quotation(person, quote);
